# Prevents peer dependency install failures on modern UI stacks
legacy-peer-deps=true

# Improves consistency across installs (CI / local)
fund=false
audit=false

# Makes installs slightly faster and less noisy
loglevel=error

# Ensures consistent package-lock behavior
package-lock=true